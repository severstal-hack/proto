syntax = "proto3";
package com.example.grpc;

message Tender {
  string link = 1;
  string domain = 2;
}

message AddRequest {
  repeated Tender tenders = 1;
}

message AddResponse {
  int32 count = 1;
}

service DataService {
  rpc AddLinks(stream AddRequest) returns (AddResponse);
}